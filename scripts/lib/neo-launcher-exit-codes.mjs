/**
 * Launcher-safe Neo runtime exit codes (dependency-free).
 * Keep in sync with src/neo-runtime/neo-runtime-exit-codes.ts (see contract tests).
 */

/** Unsupported Node runtime — must match NEO_RUNTIME_EXIT_UNSUPPORTED_RUNTIME. */
export const NEO_RUNTIME_EXIT_UNSUPPORTED_RUNTIME = 3;
